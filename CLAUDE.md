# GST CS Global 대시보드 — 작업 지침

구글시트를 읽어 스크러버 설비의 설치·고장·자재·인원·교육을 보여주는 사내 대시보드.
정적 HTML(GitHub Pages) + Supabase 엣지펑션 + 카카오톡 챗봇으로 이뤄져 있다.

여기 적은 것은 **모르고 손대면 사고가 나는 것들**이다. 코드를 고치기 전에 읽어라.

---

## 제1원칙 — 시트 열 번호를 코드에 박지 않는다

열은 **헤더 이름으로 찾는다.** `assets/core.js`의 `GST.SM`이 그 일을 한다.

```js
const m = GST.SM.map(rows, GST.SM.SPEC.wk);   // 수선실적
const C = m.C;                                 // {stage: 13, alarm: 25, ...}
rows.slice(m.hi + 1).forEach(r => r[C.stage]);
```

**왜 이렇게까지 하는가.** 교육현황 시트에 Scrubber Lv.2/Lv.3 두 열이 새로 들어왔을 때
비고가 16→18열로 밀렸다. 코드는 계속 16열을 읽었고, **에러 하나 없이** 퇴사자가
재직자로 되살아났고 저장은 엉뚱한 칸에 들어갈 뻔했다. 조용히 틀리는 게 가장 위험하다.

규칙 셋:

- **정규화 후 정확일치.** 부분일치를 쓰지 않는다 — `입사일`이 `재입사일`을,
  `no`가 `note`를 잡던 사고가 실제로 있었다.
- **이름이 바뀔 수 있으면 별칭 배열로.** `site: ['Site', '교육과정']`
- **못 찾으면 조용히 넘어가지 않는다.** 옛 열 번호로 폴백하지 말고 `miss`에 남긴다.
  화면에는 붉은 배너가 뜨고, `GST.SM.panel()`로 어떤 항목이 몇 번 열로 잡혔는지 볼 수 있다.

헤더 비교에는 반드시 `GST.SM.norm`(브라우저) / `hnorm`(hr.js, sheet-write)을 쓴다.
시트 머리글에는 줄바꿈이 들어 있다 — `Scrubber⏎S/N`, `Main Tool⏎Maker`.
`trim().toLowerCase()`로는 정확일치가 **원리적으로 불가능**하다.

---

## 제2원칙 — 스펙은 세 곳에 복제돼 있다. 고치면 셋 다 고친다

같은 시트를 세 런타임이 각자 해석한다. 파일 공유가 안 된다
(브라우저 ↔ Deno, 그리고 Supabase 웹 콘솔은 함수 폴더 밖을 못 읽는다).

| 위치 | 역할 |
|---|---|
| `assets/core.js` → `GST.SM.SPEC` | **정본** |
| `supabase/functions/kakao-bot/hr.js` | `INSTALL_SPEC` · `FAULT_SPEC` · `parseCIP` 내부 |
| `supabase/functions/sheet-write/index.ts` | `SCHEMAS` (쓰기 경로) |

세 파일 모두 해당 블록에 `SPEC-SYNC` 주석이 달려 있다.
**한 곳을 고치면 나머지 둘을 같이 고치고 `tests/t-sync.mjs`를 돌린다.**
그 테스트는 실제 시트 헤더를 세 해석기에 먹여 같은 열이 나오는지 대조한다.
갈라지면 즉시 실패하고 어디가 어긋났는지 알려준다.

---

## 시트별 함정 (전부 실측 확인)

**교육현황 (gid 0) — 3단 머리글이고, 2026-08에 «헤더가 맨 아래»로 뒤집혔다**
```
r0                                  법인 교육과정(4:5 병합)   본사 교육과정(6:7 병합)
r1                                  Basic(4) Veteran(5)      Scrubber Lv.2(6) Scrubber Lv.3(7)
r2  No(0) Site(1) 인원(2) 사원번호(3) 교육완료일(4) 교육완료일(5) 교육완료일(6) 교육완료일(7)   ← 헤더행
r3~ 데이터
```
`완료일`이 4·5·6·7 네 곳이라 **이름만으로는 구분이 불가능하다.** 상위 밴드로 범위를 좁힌 뒤
그 안에서 찾는다(`sheet-write`의 `{band, sub}` ColRef, `hr.js`·`hr/index.html`의 `eduMap`).

**개편 전에는 헤더행이 r0이고 밴드가 그 «아래»였다.** 그래서 밴드 탐색은 헤더행의
**위아래를 모두**(`hi-2 ~ hi+2`) 훑어야 한다 — 한쪽만 보면 한 레이아웃에서 반드시 깨진다.

- **헤더행은 `인원` + `사원번호`로 찾는다.** 예전에는 `인원` + `교육과정`을 요구했는데,
  개편 후 `법인 교육과정`은 r0, `인원`은 r2라 **둘 다 있는 행이 없어** `eduMap`이 `null`을
  반환했고 **교육 지표가 통째로 빈칸이 됐다**(에러는 하나도 안 났다).
- **없어진 열: `입사일`·`경력`·`직급`·`직무`·`이수여부`·`시작일`·`교육시간`·`비고`.**
  인적사항은 **인원현황(1213453343)이 원장**이고, **이수 판정은 완료일 유무**,
  **퇴사 판정은 인원현황 `Resignation`** 하나로 일원화됐다.
- **못 찾은 열을 옛 번호로 폴백하지 말 것.** 예전 `eduMap`은 `입사일`을 못 찾으면 4열을 썼는데,
  개편 후 4열이 **Basic 완료일**이라 완료일을 입사일로 읽게 된다. 제1원칙 그대로 — `-1`을 낸다.
- 이 레이아웃 해석기는 **세 곳에 복제**돼 있다(`hr/index.html`·`report/index.html`·
  `kakao-bot/hr.js`). `tests/t-edu.mjs`가 셋을 각각 떼어 **옛·새 레이아웃 양쪽**에 먹인다.

**설치현황 (gid 891608329) — 128열, 이름이 중복된다**
`Type`이 44·79·96, `Main Tool ID`가 17·30, `N2 Purge (slm)`이 22·25·49·53에 있다.
챔버 판정은 반드시 **`Scrubber type`**(75열)으로 잡는다.
`Type`으로 잡으면 44열을 물어 전 설비가 SINGLE로 계산된다.

**CIP (gid 2123129719 F11 · 1999732389 F16) — 점검 항목이 열로 늘어난다**
항목 구간을 숫자로 박지 말 것. **`FAB In` 다음 ~ `Remark` 직전**으로 유도한다
(`GST.SM.cipRange`). 그래야 항목이 추가돼도 코드를 안 고친다.
헤더는 0행이 아니라 **1행**이다(0행은 적용일자 띠).

**수선실적 · 자재실적** — 헤더 단일 행, 정규화 후 중복 없음. 평면 매칭으로 충분하다.

---

## 데이터 출처가 둘이다 — 미러(Supabase)와 시트 (v77)

구글시트 셀 한도에 걸려 **조회를 Supabase 미러로 옮겼다.** 쓰기는 그대로 시트로 간다.

| gid | 시트 | 지금 어디서 읽나 |
|---|---|---|
| 646668307 · 31302669 · 891608329 | 수선·자재·설치 | **미러** `sheet_wk` · `sheet_mat` · `sheet_inst` (sheet-sync 가 30분마다) |
| 0 · 1213453343 · 262805841 | 교육·인원·휴가 | **CSV Import 표** `sheet_edu` · `sheet_roster` · `sheet_leave` |
| 2123129719 · 1999732389 · 1263412805 | CIP F11·F16 · ABP | **CSV Import 표** `sheet_cip_f11` · `sheet_cip_f16` · `sheet_abp` |

**두 경로가 다르다.** 미러는 `sheet-sync` 가 자동으로 채우고 컬럼이 snake_case 이며
`sheet_sync_log` 로 행수를 대조한다. CSV Import 표는 **사람이 Table Editor 로 올리고**
컬럼 이름이 **시트 머리글 그대로**다. `GST.TABLE_OF_GID`(미러) 와
`GST.CSV_TABLE_OF_GID`(Import) 로 갈린다 — **같은 gid 를 양쪽에 넣지 말 것.**
어느 쪽을 읽는지가 코드 순서에 달리고, 나중에 «왜 옛 값이 보이나»로 돌아온다
(`tests/t-csvdb.mjs` 의 [3]번이 막는다).

**Import 하면 모양을 잃는다 — core.js 가 되살린다.** Table Editor 는 머리글을 한 줄만 받는다.
그래서 CIP 의 «적용일자 띠»(시트 0행)와 ABP 의 크로스탭이 사라진다.
`GST._cipBand` 는 **열별 최초 완료일**로 띠를 되살리고(2010년 이전은 버린다 —
시트에 `1901-02-19` 오타가 있고 그걸 쓰면 경과일이 4만 5천 일이 된다),
`GST._abpWide` 는 세로형을 크로스탭으로 되편다.
**페이지·파서는 한 줄도 안 고쳤다** — 고치면 시트 경로와 DB 경로가 갈라진다.

- 페이지는 **한 곳도 안 고쳤다.** `GST.fetchCSVCached`가 gid를 보고 갈라진다
  (`GST.TABLE_OF_GID`). 첫 행에 시트 헤더 이름을 얹어 2차원 배열로 돌려주므로 `GST.SM`이 그대로 돈다.
  맵에 없는 gid는 시트로 간다 — 그래서 부분 이관이 자연스럽다.
- **되돌리기는 `GST.USE_DB=false` 한 줄.** 시트는 하나도 안 건드렸다.
- **컬럼 매핑을 엣지펑션에 적지 말 것.** `sheet-sync`는 `sheet_spec`·`sheet_colmap` 표를
  읽어 헤더를 해석한다. 함수에 적으면 스펙의 **네 번째 사본**이 된다.
  두 표는 `supabase/gen-ddl.mjs`가 `GST.SM.SPEC`에서 뽑는다.
  **시트에 열이 늘면**: core.js SPEC 수정 → `node supabase/gen-ddl.mjs` →
  나온 SQL을 Run → `npm run mirror`. 함수는 재배포하지 않는다.
- **PK는 `src_row`(시트 행번호)다.** 실적코드·Scrubber CODE를 키로 쓰면 안 된다 —
  실측상 빈값 246·162건에 중복도 있다. 근거는 `setup-4-tables.sql` 상단 주석.
- **페이지네이션을 "요청한 만큼 안 오면 끝"으로 판정하지 말 것.** PostgREST의 행수 상한은
  프로젝트 설정이라, 상한에 걸린 것을 완료로 착각하면 잘린 데이터를 조용히 그린다.
- 미러가 3시간 넘게 안 돌았거나 마지막 적재가 실패하면 화면에 밝힌다(`GST._dbBanner`).
  **읽기는 성공하는데 값이 옛날 것**인 경우가 가장 설명하기 어려운 실패다.

## 배포 순서 (틀리면 화면이 안 뜬다)

1. **`assets/core.js` 먼저.** 페이지들이 배포본 core.js를 절대경로로 부르므로,
   core.js가 구버전이면 `GST.SM`이 없어 아무것도 렌더되지 않는다.
2. 그다음 페이지들 (`fault` · `material` · `pm` · `scrubber` · `tco` · `report` · `cip` · `hr`)
2-1. **SQL 을 먼저.** 새 표를 쓰는 화면은 표가 없으면 «비우기»에서 `bad_table` 로 막힌다 —
   데이터는 안전하지만 일이 멈춘다. 순서는 `setup-4` → `5` → `7` → `8` → `9` → `10`.
3. Supabase는 **별개**다. `sheet-write`·`kakao-bot`·`sheet-sync`·`sheet-proxy`는
   콘솔에서 코드 교체 후 Deploy. GitHub에 올린다고 반영되지 않는다. SQL도 마찬가지 —
   `setup-4-tables.sql` → `setup-5-sync-rpc.sql` 순서로 Run
   (자세한 것은 `supabase/functions/sheet-sync/DEPLOY.md`).
4. **엣지펑션끼리도 순서가 있다 — `sheet-proxy`가 먼저다.** `sheet-sync`·`kakao-bot`이
   구글이 아니라 `sheet-proxy`를 부르므로(v78), 뒤집으면 새 호출자가 옛 프록시를 만나
   **401로 전부 멎는다.** `supabase/functions/sheet-proxy/DEPLOY.md`

---

## 데이터 공백 — 차트가 비어 보이는 게 버그가 아닐 수 있다

| 열 | 입력률 | 대응 |
|---|---|---|
| 수선실적 `알람유형` | 0.3% (고장 BM 기준 4%) | 차트에 입력률 안내 표시. 30% 넘으면 자동으로 사라짐 |
| `현상` · `원인` · `조치` | BM 기준 약 10% | 위와 동일 |
| 자재실적 `PART` · `UNIT` · `ASSEMBLY` | **전부 공란** | `자재명`으로 대체 중 |
| 자재실적 `단가` | 전 행 0 | 금액 분석 불가 |

알람·원인 열은 현장에서 채워 나가는 중이다. 비었다고 코드를 고치지 말 것.

사용주기는 시트가 이미 계산해 둔 `사용일(유상기준)` · `사용일(전교체일기준)`을 쓴다.
직접 계산하지 않는다.

---

## 알려진 환경 제약

- **git push는 된다** (2026-08-13 확인, 브랜치 `claude/dashboard-improvements-ftx2rh`).
  예전 세션에서 프록시 403이라 zip으로 전달하던 시절이 있었는데 지금은 아니다 —
  먼저 push를 시도할 것. 정말 막히면 그때 zip으로 돌아간다.
- **저장소가 공개다.** 실데이터(작업자 실명·고객사·설비호기)를 커밋하지 말 것.
  `.gitignore`는 픽스처 **파일**만 막는다 — **주석에 예시로 적어 넣는 것은 못 막는다.**
  실제로 그렇게 새어 나갔다 — 설명 주석에 든 실제 S/N 하나와 작업자 실명 하나.
  화면에 안 나와도 저장소에는 남고, 지워도 git 이력에 남는다. `tests/t-leak.mjs`가 이제 막는다.
- 대시보드는 Supabase 인증 뒤에 있다. 시트는 `sheet-proxy`를 거쳐 읽는다.

## 구글시트 접근 — 웹게시는 인증이 없다 (v78)

**`sheet-proxy`가 구글시트를 읽는 유일한 통로다.** 서비스 계정(Sheets API v4)으로 읽는다.
`sheet-sync`·`kakao-bot`은 구글이 아니라 **`sheet-proxy`를 부른다**(`x-sync-secret`).
구글 자격증명(`GS_SA_*`·`GS_SHEET_ID`)은 `sheet-proxy`·`sheet-write` **둘만** 갖는다.

- **왜.** 예전에는 셋 다 웹게시(publish to web) CSV를 받았다. **웹게시에는 인증이 없다** —
  URL만 알면 누구나 전량을 받는다(작업자 실명·고객사·설비 S/N). 옛 주석의 *"URL이 secret이라
  노출되지 않음"* 은 **가린 것이지 보호한 게 아니었다.**
- **읽기 방식을 복제하지 말 것.** 세 함수에 흩어놓으면 제2원칙의 사고가 재현된다.
  `sheet-proxy`의 「구글 읽기」 블록은 `sheet-write`와 한 벌이고 `SPEC-SYNC` 주석이 달려 있다.
- **`FORMATTED_VALUE` 고정.** `UNFORMATTED_VALUE`로 바꾸면 날짜가 시리얼 숫자(45000)로 와서
  **모든 날짜 컬럼이 조용히 깨진다.** `values.get`은 꼬리 빈칸을 생략하므로 **직사각형 패딩**도 필수다.
- **gid 허용목록**(`ALLOW_GID`)에 없으면 400이다. 화면에는 그 시트만 조용히 빈다 —
  `tests/t-mirror.mjs`의 [6]번이 소비자 전부를 덮는지 대조하므로 잊으면 거기서 걸린다.
- `GS_SHEET_ID`는 편집 주소의 `/d/<여기>/edit`다. 웹게시의 `/d/e/2PACX-…`를 넣으면 404 —
  가장 흔한 설정 실수다.

**미러가 원장이 아닌 동안에는 쓰기가 두 곳에 간다.** `sheet-write`는 시트에 쓴 뒤
같은 행을 미러에도 넣는다(`mirrorWrite`). 행은 **업무 키로 찾고 정확히 한 행일 때만** 쓴다 —
미러 PK `src_row`는 `sheet-sync`가 **빈 행을 걸러낸 뒤의 순번**이라 A1 행번호에서 유도할 수 없다.
그 규칙을 복제해 어긋나면 **남의 행을 덮는다.**

## 쓰기 경로가 둘로 갈라져 있다 (v80)

`GST.sheetWrite`가 gid를 보고 갈라진다 — 페이지는 모른다.

| gid | 표 | 쓰기 경로 |
|---|---|---|
| 0 · 1213453343 · 262805841 | 교육·인원·휴가 | **브라우저 → Import 표 직접** (`GST.dbWrite`, RLS가 권한 — `setup-7-write-policies.sql`) |
| 646668307 | 수선실적 dq 4열 | **브라우저 → `sheet_wk` 직접** (v82 — `ops:['perm','row','update']`, 행 생성·삭제 금지: 실적 원장은 CSV 업로드다) |

`sheet-write` 엣지펑션은 이제 대시보드가 부르지 않는다(은퇴 후보). 지우기 전에
kakao-bot의 카톡 점검입력(v72 계획)이 실제 배포됐는지부터 확인할 것.

- **왜.** 교육·인원·휴가는 읽기가 Import 표로 옮겨졌다(v79). 쓰기만 시트로 가면
  «저장했는데 새로고침하면 사라지는» 상태가 된다 — 화면이 시트를 더 이상 안 본다.
- `GST.dbWrite`는 sheet-write의 op 규약(perm·row·update·append·delete)과 **응답 모양을
  그대로** 흉내낸다. hr 페이지는 한 줄도 안 고쳤다. 409 충돌·캐스케이드(사번/이름 변경 시
  교육 표 동기화)·정확히-한-행 규율 전부 유지 — `tests/t-dbw.mjs`가 26케이스로 지킨다.
- **RLS 거부는 에러가 아니라 0행 반환이다.** `.select()` 결과가 1행이 아니면 던져야 한다 —
  조용히 넘기면 «저장됨»으로 보이는데 DB는 그대로다(t-dbw [8]이 막는다).
- `GST.DBW`의 컬럼 이름은 Import 표의 실제 열 이름이다(시트 머리글 그대로).
  표 열이 바뀌면 여기와 `t-dbw.mjs`의 스키마 상수를 같이 고친다.

## 신규 데이터는 CSV 수동 업로드다 — cron 은 은퇴했다 (v81)

사용자 결정: 실적 3종 포함 **전 시트를 CSV 수동 업로드**로 넣는다(`/upload/` 페이지).
`setup-8-csv-upload.sql`이 sync-wk·mat·inst cron 을 정지시키고 업로드 RPC 를 만든다.

- **업로드 화면의 변환은 sheet-sync `toRows`와 같은 규칙이어야 한다** —
  src_row(빈 행 제거 후 순번)·빈칸→null·미지정 열→extra. `t-upload.mjs`가
  실제 페이지를 띄워 배포 sync 코드와 **전 행 전 칸** 대조한다. 갈라지면 같은
  데이터인데 화면 숫자가 달라진다.
- **`sheet_sync_log.ms = -1` 은 수동 업로드 모드 표식이다.** `csv_upload_finish`가
  남기고, `dbRows`가 이를 보고 «3시간 미갱신» 나이 경고를 끈다 — cron 이 없으니
  오래된 것이 정상이다. 이 표식 없이 업로드하면(로그 미갱신) `MIRROR_SHORT`로 화면이 막힌다.
- **업로드는 비우고-다시-넣기(truncate → insert)다.** 중간 실패 시 표가 빈 채로
  남을 수 있다 — 같은 파일로 재업로드하면 복구된다. 부분 병합이 아니므로
  «파일이 곧 전체 상태»다. 잘린 파일을 올리면 데이터가 준다(화면이 절반 미만이면 경고).
- **여러 번 나눠 올릴 때는 «구간 교체»를 쓴다(v87).** 그냥 이어붙이면 겹치는 구간이
  이중 계산된다 — 실측: 새 추출본의 대만분 6,534행 중 **6,266건이 기존 표에도 있는 실적코드**였다.
  `csv_window` RPC 가 «파일의 날짜 범위 × 파일의 운영단위 집합»에 해당하는 기존 행만 지우고
  파일을 넣는다. 대만 2025년 이력은 남고 겹치는 2026년만 갈아끼워진다.
  · **키(실적코드) 기반 병합을 만들지 말 것.** 자재실적에는 자연키가 없다 —
    (수선실적번호+자재코드+자재위치+일자) 조합조차 실측 중복 59건이다. 수선만 키를 쓰면
    두 표가 다른 규율로 갈라진다(제2원칙).
  · 미리보기(dry)와 실행이 **같은 함수·같은 조건식**을 쓴다. 두 벌로 나누면 "삭제 예정 100행"
    이라 해놓고 다른 수를 지우는 날이 온다.
  · 날짜는 앞 10자만 비교한다 — 시각이 붙어 들어온 행도 같은 날로 잡아야 «그 날짜를 덮었다»가 지켜진다.
  · 삭제 후 `src_row` 는 `max+1` 부터 이어 매긴다. 파일 내 순번을 그대로 쓰면 **남은 행의 PK와 충돌**한다.
  · 설치현황은 «그 시점의 전체 명부»라 구간이라는 개념이 없다 → 통째 교체만 (`_csv_datecol` 이 null).
- **넣는 방식은 셋이다(v89).** 통째 교체 · 겹치는 구간만 교체 · **겹치는 구간도 그대로(이어붙이기)**.
  세 번째는 아무것도 지우지 않는다 — 사용자가 «지우는 것이 무섭다»고 할 때의 안전한 선택지다.
  대신 **겹치면 반드시 이중 계산된다**. 그래서 미리보기에서 `csv_window`(dry)로 «이 파일이 덮는
  기간·사업부에 기존 행이 몇 개 있는지»를 세어 붉게 알린다 — 구간 교체와 **같은 조건식**이라
  마음을 바꿔도 숫자가 달라지지 않는다. `src_row` 는 `max+1` 부터(PK 충돌 방지).
- **비우기 «전에» 표의 실제 컬럼과 대조한다**(`csv_table_cols` RPC). 콘솔에서 `extra`
  컬럼을 지운 채 업로드해 **비우기는 되고 적재가 실패, 표가 빈 채로 남은** 사고가 있었다.
  필수 열이 없으면 아예 진행하지 않고, `extra` 만 없으면 그 값을 빼고 진행한다.
  컬럼 확인을 «한 행 받아 키 보기»로 하면 안 된다 — 표가 비어 있을 때 못 쓰는데,
  하필 그때가 이 검사가 가장 필요한 순간이다.
- **미러 표는 Table Editor Import 가 안 된다.** 컬럼이 snake_case(`code`·`fab_in`)라
  시트 머리글(`Scrubber CODE`·`FAB In`)과 이름이 달라 «not present in your table» 이 뜬다.
  Import 표(교육·인원·휴가)만 머리글 그대로다. 실적 3종은 반드시 `/upload/` 를 쓴다.
- **카카오톡 챗봇도 Supabase 를 읽는다 (v82).** kakao-bot 의 `fetchCsv`가 시트 대신
  표를 읽어 «시트 모양 CSV»로 되살린다 — 파서(hr.js)·bot_cache·라우팅은 무수정.
  미러 표의 머리글은 `sheet_colmap`에서 되살린다(엣지펑션에 스펙을 적으면 네 번째
  사본이 된다). `tests/t-botdb.mjs`가 원본 CSV 파싱과 표 복원 CSV 파싱을
  **레코드 단위로** 대조한다(수선 17,091 · 설치 1,490 · CIP 4,700).
  **sync-kakao-bot cron 은 계속 돌아야 한다** — 이제 시트가 아니라 DB 에서
  bot_cache 를 갱신하는 유일한 경로다.
- 시트 파일은 읽기전용 보관본이다. `sheet-proxy`는 DB 실패 시 폴백으로만 남아 있다 —
  구글 자격증명(`GS_SA_*`)을 폐기하면 그 폴백이 죽는다는 것을 알고 하라.

---

## 시트에 열이 늘어날 때 (v89 — 예정된 일이다)

사용자가 예고했다: 설치현황에 **LNG·O2·CDA 사용량**, **옵션** 등이 앞으로 추가된다.
다른 시트도 마찬가지다. 그때 기존 데이터를 안 깨는 순서가 있다.

**먼저 알 것 — 아무것도 안 해도 값은 안 잃는다.** 업로드 화면은 SPEC 에 없는 열을
전부 `extra` jsonb 에 담는다(`extra` 를 지우면 안 되는 이유가 이것이다). 그래서
새 열이 붙은 CSV 를 그냥 올려도 **적재는 성공하고 값도 보존된다.** 다만 `extra` 안에
있는 동안은 화면·필터·챗봇이 못 쓴다. 화면에서 쓰려면 «승격»한다.

**승격 순서 — DB 가 먼저다.** 반대로 하면 업로드 화면의 «비우기 전 컬럼 대조»가
`표에 없는 컬럼` 으로 막는다. 데이터는 안전하지만 일이 멈춘다.

1. `alter table public.sheet_inst add column if not exists lng_slm text;`
2. `assets/core.js` 의 `GST.SM.SPEC.<표>.fields` 에 추가 —
   **반드시 같은 스펙의 `opt` 배열에도 넣는다.** `opt` 는 «없어도 정상»이라는 표시다
   (`GST.SM.map`: `if(idx<0 && opt.indexOf(k)<0) miss.push(...)`). 안 넣으면 그 열이 없는
   **옛 추출본이 전부 «열을 못 찾았습니다»로 거부된다** — 픽스처와 테스트도 같이 죽는다.
3. `kakao-bot/hr.js` 의 대응 SPEC 도 같이 (제2원칙 · `t-sync` 가 대조한다).
4. `node supabase/gen-ddl.mjs` → 나온 ALTER/시드 SQL 을 Run.
   **`sheet_colmap` 갱신이 여기 있다** — 챗봇은 미러 표에서 «시트 모양 CSV»를 되살릴 때
   이 표로 머리글을 복원한다(v82). 빠뜨리면 봇만 그 열을 못 본다.
5. `/upload/` 로 **전 기간 재업로드.** 옛 행은 그 열이 `null` 이다 — 승격만으로는
   과거가 안 채워진다.

**이름을 유일하게 지을 것.** 설치현황은 이미 `Type` 3곳 · `Main Tool ID` 2곳 ·
**`N2 Purge (slm)` 4곳**이다. LNG·O2·CDA 는 십중팔구 그 옆에 같은 패턴으로 붙는다 —
`사용량` 같은 모호한 이름을 쓰면 제1원칙의 사고가 그대로 재현된다.
`LNG (slm)` 처럼 단위까지, 설계값·실사용량이 둘 다면 `LNG Spec (slm)`·`LNG Actual (slm)`.

**«개수가 계속 느는 축»은 열로 붙이지 말 것.** 옵션이 그렇다. 열로 붙이면 CIP 시트가
된다(항목이 늘 때마다 코드를 고쳐야 하는 상태). 소수 고정이면 열, 계속 늘면 한 열에
구분자로 몰아 넣거나 CIP 처럼 **구간 유도**(`X 다음 ~ Y 직전`)로 처리한다.

## 국내 알람·올바이패스는 실적이 아니라 별도 원장이다 (v92)

**사용자 결정: 국내는 BM·All By-Pass 를 수선실적에서 세지 않는다.** 수선실적의 BM 정합성이
아직 안 맞아서, 현장이 실제로 쓰는 「CS 알람관리」 워크북을 원장으로 삼는다.
**대만은 지금까지대로** 수선실적 BM + ABP 시트를 쓴다 — 두 계통이 공존한다.

| | 대만 | 국내 |
|---|---|---|
| Alarm(BM) | 수선실적 `작업단계=BM` | `sheet_alarm` (`cnt=true`) |
| All By-Pass | ABP 시트(크로스탭) | `sheet_allbypass` (`cnt=true`) |

- **원장이 비어 있으면 옛 경로 그대로 간다**(`KR_ON`). 올리기 전후로 대만 숫자가 한 자리도
  안 움직이고, 국내는 올린 뒤부터 나온다. 비었는데 빼 버리면 국내가 통째로 0이 된다.
  · **다만 «조용히» 돌아가면 안 된다 — 실제로 몇 시간을 잃었다.** `sheet_alarm` 이 0행인 채로
    올바만 올라가 있었는데(실측: 알람 0 · 올바 5,068), 화면은 국내를 수선실적 BM 으로 세면서
    아무 말도 안 했다. 수선실적은 알람유형 입력률이 **0.3%** 라 「미기재」가 잔뜩 뜨고,
    사용자는 원장을 보고 있다고 믿어 «있는 데이터가 안 나온다»로 읽었다.
    → `window._KRWHY` 에 «안 올림 / 못 읽음»을 구분해 남기고, Alarm 차트 주석 맨 앞에
    «원장이 비어 수선실적으로 세는 중 · /upload/ 에서 올리면 원장 기준으로 바뀝니다» 를 적는다.
    **증상만 알리면 사용자가 코드를 의심한다 — 무엇을 하면 되는지까지 적는다.**
  · 적재 여부는 `supabase/check-alarm.sql` 한 방으로 판정된다(기대값이 항목 이름에 박혀 있다).
  · **알람과 올바이패스는 «열이 다른 두 표»다 — 조회 열 목록을 공유하지 말 것.**
    실제로 한 벌로 공유해서 `sheet_alarm` 읽기가 «column sheet_alarm.seq does not exist» 로
    **매번 통째로** 실패했다. PostgREST 는 select 에 없는 열이 하나라도 있으면 그 열만
    비우는 게 아니라 **전체를 거부한다.** 그 실패가 «원장이 비었다»로 판정돼 국내가 조용히
    수선실적으로 계산됐다 — 업로드를 마친 뒤에도 증상이 똑같아 원인을 두 번 헛짚게 만든다.
    `seq` 는 올바이패스에만(H 는 한 사건이 Seq 1·2·3) · `alarm_name` 은 알람에만(K 249행).
    `GST._KR_COLS_A`/`_B` 로 갈라 두었고, `t-alarm` 이 `setup-10-alarm.sql` 의 실제 컬럼과
    대조한다(음성 대조로 잡히는 것을 확인했다).
- **원장에 해외 설비로 조인되는 행이 있으면 뺀다.** 안 그러면 대만이 «수선실적 BM + 원장»
  으로 이중 계산된다. 몇 행을 뺐는지 차트 노트에 적는다(조용히 빼지 않는다).

**한 워크북에 K·P·H 세 운영단위가 «각자 다른 양식»으로 들어 있다.** 같은 뜻의 열이
사이트마다 다른 이름이다 — `알람 유형`/`알람분류`/`대분류`, `외부 / 내부`/`내적/외적`/`구분기준`,
`S/N`/`SEQP S/N`/`SEQP ID`. 헤더 행도 0행과 1행이 섞여 있다. 흡수는 `GST.SM.SPEC.alarm`·`.abp2`
의 **별칭 배열 한 곳**에서 한다 — 사이트별 코드를 만들지 말 것(제2원칙).

지킬 것 다섯:

- **집계 대상 판정을 새로 발명하지 말 것.** 시트가 이미 자기 답을 열로 갖고 있다 —
  P는 `내적 / 제외`, K·H는 `외부 / 내부`·`내적/외적`. `GST.ALARM.counts` 가 그 열을 읽는다.
  **알람·올바이패스 둘 다 「내부·내적」만 센다**(사용자 확정). 한쪽만 다른 규율을 쓰면
  «Alarm 은 내적만인데 올바는 전부» 라는 설명 불가능한 표가 된다.
- **판정은 둘로 쪼개 둔다 — `dedup`(한 사건의 첫 줄) · `inner`(내부·내적).** 화면의
  「내/외」 필터가 «전체»를 고를 수 있어야 하는데, 그때도 `dedup` 은 걸려야 한다.
  한 함수에 묶으면 «전체»를 고른 순간 H 올바이패스가 정확히 3배로 부푼다
  (`t-alarm` 이 report 의 `krPick` 이 dedup 을 먼저 거는지 소스로 확인한다).
- **H 올바이패스는 한 사건이 `All-ByPass Seq` 1·2·3 세 줄이다**(실측 각 918행). 그냥 세면
  건수가 정확히 3배가 된다. Seq=1 만 한 사건이다 — `t-alarm` 이 음성 대조로 지킨다.
- **정산월과 발생일은 «다른 달»이다 — 화면에서 고를 수 있게 해 뒀다.** 실측 H 알람에서
  둘이 어긋나는 행이 173건, H3 단지 8월만 봐도 정산월 96 ↔ 발생일 31 로 3배 차이가 난다.
  시트와 손계산을 대조할 때는 «발생일», 현장 보고와 맞출 때는 «정산월»이다
  (`sl-krbasis`). **기본은 발생일이다** — 정산월은 삼성 정산 기준일 뿐이고, 「8월에 몇 건」을
  물을 때 사람이 뜻하는 것은 발생일이다. 대만 막대도 발생일로 서 있어 정산월로 세면
  한 차트 안에서 두 축이 섞인다. 발생일 모드의 주차는 화면의 주차 규약(`isoW` = 엑셀
  WEEKNUM)으로 다시 센다 — 원장만 ISO 주차를 쓰면 같은 막대에서 한 주가 어긋난다.
- **«단지별» 이라고 이름 붙인 카드의 드릴도 단지로 찍는다 (v96).** 입·퇴사 드릴만
  `x.fab||x.custB` 로 내려가고 있어서 대만 인원이 단지 F16 대신 고객사 MICRON 으로,
  국내가 SAMSUNG 으로 찍혔다 — 머리글은 「라인」이라 한 표에 세 낱말이 섞였다.
  드릴은 차트가 쓰는 키(`_DRILL.grpRaw`)를 그대로 받아 쓴다. 같은 카드의 막대와 목록이
  다른 축을 쓰면, 눌러 본 사람만 그 사실을 알게 된다.
- **설치현황은 국내·해외 양식이 다르다 — 별칭 배열 한 곳으로 흡수한다 (v96).**
  국내 74열(한글) · 해외 118열(영문). ⚠ **`FAB` 이 두 양식에서 다른 뜻이다** —
  해외 FAB=F16·FAB1(화면의 라인), 국내 FAB=FSF·R/P·CSF(공장 구역)이고 화면의 라인은
  국내에서 `Line 1` 이다. 그래서 별칭 **순서가 규약**이다(`fab:['Line 1','FAB']`).
  뒤집으면 국내 라인 축이 통째로 FSF/R/P 가 된다. 챔버 판정도 이름이 다르다 —
  해외 `Scrubber type` · 국내 `Type2`(국내 `Type1` 은 버너 방식이지 챔버 수가 아니다).
  사업부(`div`)는 국내에만 있어 `opt` 이고 필터에서는 `loose` 다 — 안 그러면 사업부를
  고르는 순간 해외 설비가 통째로 사라진다.
- **단지·FAB 이 둘 다 비면 «미배치»다 (v96 · 사용자 확정).** 짐작으로 다른 축의 값을 빌려
  오지 않는다 — 예전에는 고객사까지 내려가 「SAMSUNG」 이 단지 막대로 섰다(실측 14명).
  한 축에 두 차원이 섞이는 것이고, 사용자가 «인원현황에 삼성이라는 단지는 없다»고 바로
  짚었다. 그렇다고 버리지도 않는다 — 버리면 합계에서 조용히 사라진다.
- **국내의 교육 축은 Scrubber Lv.2 · Lv.3 이다 — 표·드릴·KPI 어디서도 예외가 없다 (v96).**
  `clsB`/`clsV` 를 직접 부르는 자리가 있으면 국내 화면이 「비대상」으로만 가득 찬다
  (실측: hr 상세 명단 399명 전원 비대상). 「그 사람의 1·2단계」를 묻는 접근자
  `eduCls1`/`eduCls2` 만 쓰고, **머리글도 `eduNames` 로 잡힌 인원을 따라간다.**
- **인원 계열의 구분 축은 «단지»다 (v94).** 해외는 고객사명이 곧 FAB(F16·F11) 이라 고객사로
  갈라도 보이지만, 국내는 전부 «삼성» 한 덩어리라 H1~H4·P1~P4 가 통째로 안 보였다.
  `_grpRaw` 는 단지 → FAB → 고객사 순으로 내려간다 — 버리지 않는다(빈 축은 합계에서 사라진다).
- **국내 원장에 «Alarm Message 미기재» 는 있을 수 없다** (실측 P·H 공란 0 · K 는 249행이
  Comment 대신 「알람」 열에만 있어 폴백한다). 그런데도 화면에 공란이 보이면 **시트가
  아니라 적재 문제**다 — 그 둘을 화면이 구분해 주지 않으면 사용자가 시트를 헛되이 다시
  뒤진다. 그래서 공란이 있으면 TOP3 아래에 «적재 문제입니다» 라고 건수와 함께 적는다.
- **단지·라인의 기준을 화면에서 고를 수 있다 (v94 `sl-krorg`).** 기본은 설치현황(정본),
  현장 손계산(시트 필터)과 맞출 때만 «알람시트 Site 기준»으로 바꾼다. 전환은 접근자
  두 개(`krCampusOf`·`krLineOf`)에서만 갈린다 — 필터·분해·드릴이 각자 고르면 카드끼리
  다른 단지를 보여준다(`t-alarm` 이 직접 선택의 부활을 막는다).
- **시트의 Site 와 설치현황의 단지가 갈리면 «설치현황» 이 맞다** (사용자가 설비 한 대를
  직접 대조해 확정했다 — 알람 시트는 H3, 설치현황은 H4, 실물이 H4). 실측 H3 96건 중
  39건이 이 경우이고 전부 H3→H4 한 방향이라, 이설된 설비 묶음의 Site 가 시트에서
  안 고쳐진 것이다. 조직 축의 정본은 설치현황이라는 규약(CIP 와 같다)을 그대로 지키되,
  **몇 건이 갈렸는지 차트 노트에 적는다** — 「왜 내 손계산과 다르지」의 답이 그 숫자다.
  ⚠ 단지 필터를 걸었을 때만 갈린다. 걸지 않으면 합은 시트와 정확히 같다(실측 H 8월 97).
- **표기 정규화는 시트 값을 쓴다.** 국내는 «삼성 기준 월»이 달력과 다르다(실측: 발생일
  2/18 인데 시트의 발생월은 3월). 달력으로 다시 세면 현장 보고와 어긋난다.
  표기가 넷이라(`26년 7월`·`26_08`·`26년 04주`·`21년4W`) `GST.ALARM.ym`/`yw` 가 눕힌다.
  **한 열 안에서도 표기가 섞인다** — 옛 K 행은 월이 `12`뿐이라 `년도` 열을 붙여야 한다.
- **발생일은 세 자료형이 섞여 온다.** 실측 K 알람 7,396행 중 날짜형은 164행뿐이고 나머지는
  문자열이다. `GST.ALARM.day` 가 Date·문자열·엑셀 시리얼을 모두 받는다.
- **조직 축(구분·운영단위·고객사·단지·라인)은 원장에 담지 않는다.** 설비 S/N 으로 설치현황을
  조인해 얻는다(실측 조인율 99.9% — S/N 표기가 `AAA0001`·`AAA-0001`·`BBB-0101S` 로 갈려도
  영숫자만 남기면 붙고, 안 붙으면 채널 접미 L/R/S 를 떼고 한 번 더 본다).
  담아 두면 설치현황이 갱신될 때 두 벌이 갈라진다 — CIP 와 같은 규약이다.

## 미러는 «적재 시각»으로 캐시한다 — 나이로 자르지 않는다 (v101)

**느림의 실체는 전송이 아니라 재파싱이었다.** 브라우저는 gzip 을 자동으로 받으므로
수선실적 257,606행의 실제 전송은 **17MB** 다(실측). 그런데 **푼 JSON 은 374MB** 이고,
새로고침할 때마다 그걸 다시 받아 다시 파싱했다.

원인이 둘이었고 **둘 다 조용했다**:

- **미러 경로가 캐시를 아예 안 봤다.** `cacheLoad` 는 «시트 경로가 실패했을 때»만
  불린다. DB 경로에는 그 자리가 없었다.
- **`localStorage` 저장이 늘 실패했다.** 한도가 5~10MB 인데 데이터가 374MB 다.
  `setItem` 이 QuotaExceededError 를 던지고 `catch(e){}` 가 삼켰다 — 캐시가 없는 것과
  같은데 화면에도 콘솔에도 흔적이 없다.

→ `GST.idb`(IndexedDB)에 담고, `GST.dbRows` 가 페이지네이션 **앞에서** 짧게 끊는다.

- **무효화 열쇠는 `synced_at + 행수`다.** 나이(TTL)로 자르면 ① 안 바뀐 데이터를 버리거나
  ② 바뀐 데이터를 계속 쓴다. 적재 시각이 같으면 내용도 같다는 것은 `sheet_sync_log` 가
  보증하는 사실이다 — 짐작이 아니다.
- **`sheet_sync_log` 는 캐시가 맞아도 매번 읽는다.** 한 행짜리라 비용이 없고, 그걸 건너뛰면
  «바뀐 데이터를 계속 쓰는» 캐시가 된다. 미러 나이 경고도 여기서 나온다.
- **실패해도 막지 않되 조용하지도 않다.** IndexedDB 가 막힌 환경(시크릿 창 등)에서는
  그냥 매번 받는다 — 틀리지 않고 느릴 뿐이다. 다만 출처 배지에 «⚠ 캐시 저장 실패» 를
  띄운다. 안 그러면 «왜 느린지»를 아무도 묻지 못한다.
- `tests/t-cache.mjs` 가 실제 브라우저에서 `dbRows` 를 두 번 불러 **두 번째에 표 요청이
  0회**인지 센다. 소스로는 못 잡는다 — IndexedDB 도, 구조화 복제가 배열을 그대로
  돌려주는지도 진짜 브라우저가 있어야 확인된다. `file://` 은 오리진이 opaque 라
  IndexedDB 가 막히므로 검사가 http 오리진을 만들어 쓴다.

**아직 안 한 것:** 열 추리기(SPEC 38열 중 화면이 쓰는 것만 — 전송 17MB→9MB)와
기간 기본창. 첫 로드 비용은 그대로다.

## 설비 대수는 「설비상태」로 센다 — 날짜가 아니다 (v99)

**사용자 확정.** 예전에는 반입일(FAB In)·Turn On 날짜로 셌는데, 그 칸이 **비어 있는 설비가 있다.**
날짜가 없으면 「아직 반입 안 됨」으로 읽혀 합계에서 조용히 빠졌다. 시트는 상태를 따로 적고 있다.

| 뜻 | 설비상태 값 |
|---|---|
| 반입 (분모) | `반입완료` · `Set-up` · `Turn-off` · `Operation` |
| 가동 | `Operation` 만 |
| **아예 안 셈** | `반납` · `반출대기` · `반출완료` · `출하대기` |

판정은 `assets/core.js` 의 **`GST.EQ` 하나**다. 페이지가 각자 세면 카드끼리 모집단이 갈린다.

- **제외목록이 아니라 «반입 4종» 허용목록으로 짠다.** 사용자가 든 제외 목록에 `반출완료` 가
  빠져 있었는데, JAPAN 1대를 빼야 총합이 맞는다(피벗 실측). 허용목록이면 새 상태가 생겨도
  자동으로 제외 쪽에 붙는다.
  · 실측 검산(2026-08 피벗): AMERICA 365/364 · WUHAN 1803/1780 · XIAN 566/464 · JAPAN 311/280 —
    넷 다 각 법인 총대수와 정확히 맞는다.
- **허용목록은 CLAUDE.md 가 경고하는 형태다** — 목록에 없으면 합계에서 조용히 사라진다.
  그래서 `cls()` 가 모르는 값에 `'?'` 를 내고, **화면이 그 건수를 밝힌다**(주간현황 카드 노트 ·
  설치현황 인사이트). 조용히 빼지 않는다.
- **상태 열이 없는 옛 추출본은 옛 날짜 판정으로 되돌아간다**(`cls()` 가 `''`). 업로드 전후로
  화면이 죽는 구간을 만들지 않기 위해서다. 그 상태임을 카드 노트에 적는다.
- **날짜가 있으면 마감일도 함께 본다.** 상태는 «지금» 값이라 과거 시점을 재현하지 못한다.
  과거 마감으로 돌려 보는 화면이 아직 안 온 설비를 세면 안 된다.
- **개수는 상태, 추이는 날짜다 — 이건 못 합친다.** 상태만으로는 시계열을 만들 수 없다.
  그래서 추이 차트는 계속 날짜를 쓰고, **날짜가 없어 빠진 대수를 차트 주석에 적는다**
  (그 숫자가 곧 「채워야 할 칸」의 수다). TCO 는 가동기간이 필요해 날짜가 필수이고,
  빠진 대수를 인사이트로 밝힌다 — 조용히 빠지면 TCO 가 실제보다 작게 나온다.
  · 상태 변경일은 **이미 시트에 다 있다**(FAB In · Setup date · Turn-on date · Turn off date).
    새 열이 필요한 게 아니라 그 칸을 채우면 된다.
- **손댈 페이지는 넷뿐이다** — 설치현황 · 주간현황 · 고장분석 · TCO. 자재·PM·CIP·인원은
  설비 대수를 안 센다. 설치현황은 `filt()` **한 곳**에서 제외 상태를 걸러 KPI·차트·표가
  같은 모집단을 보게 한다.
- **승격 순서를 지킬 것**(v89 그대로): DB `alter` → core.js SPEC(`opt` 에도!) → hr.js →
  `gen-ddl` → `/upload/` 재업로드. `state` 는 SPEC 밖일 때 `extra` 에 들어가 있으므로,
  **승격만으로는 값이 안 채워진다 — 반드시 재업로드해야 한다.**

## 조직 축은 추론하지 않는다 — 시트에 적힌 값 그대로 (v98)

**사용자 확정: 운영단위·사업부·고객사를 코드가 짐작하지 않는다.**
계층은 엑셀 피벗과 같다 — **구분 → 운영단위(법인) → 사업부 → 고객사 → 단지 → 라인**.
`GST.filters` 의 축 순서·종속·사이드바 마크업이 전부 이 순서다(`t-filters` 가 셋을 다 본다).

- ⚠ **해외 설치현황의 `Country` 는 국가가 아니라 «법인명»이다**(`GST TAIWAN SCRUBBER` 등).
  진짜 국가는 `GST.ORG.country()` 가 따로 푼다.
- 주간현황 가동현황 표의 행 축은 그 두 열 원문이다. 법인을 고르면 한 칸 내려가 행이
  고객사가 되고 첫 칸이 그 법인 이름이 된다 — **머리글도 같이 움직인다**(안 그러면
  「법인」 칸에 고객사가 선다). i18n 재적용이 되돌리지 못하게 `data-i` 표식도 같이 옮긴다.
- **행 목록을 박지 말 것.** 설비가 실제로 있는 값에서 만든다 — 대만 7개로 박아 뒀을 때
  국내 설비 7,337대가 통째로 사라졌다.
- **정규화해 견주는 축은 «한 행만» 받아 가게 한다.** 고객사 축은 표기가 갈려 정규화해
  견디는데, 서로 다른 원문 둘이 같은 값으로 접히면 그 인원이 두 행에 잡혀 TOTAL 이 부푼다.
- **안 붙는 것을 억지로 잇지 않는다.** 국내는 설비가 `삼성전자`·인원이 `P1` 이라 어휘가
  다를 수 있고, 그때 **빈 칸이 곧 «자료가 없다»는 사실**이다.
- 실측(2026-08): 설치현황 법인 ↔ 인원현황 운영단위는 **낱말이 이미 같다**
  (`SEC Scrubber`·`GST TAIWAN SCRUBBER`·`SDC Scrubber`·`SK Scrubber`). 남은 문제는 셋이다 —
  ① 인원 `사업부` 584행 전부 공란 ② 인원 `운영단위` 공란 85명(칠러 제외 499명 중 17%)
  ③ 같은 표 안에서 연속 공백이 흔들린다(`GST CHINA(WUHAN)··SCRUBBER`).

## 업로드는 엑셀(.xlsx)을 그대로 받는다 (v95)

사용자 결정: **CSV 변환을 그만둔다.** 자료를 주는 사람도 싫어하고 한 단계가 더 든다.
`/upload/` 가 모든 표에서 xlsx·CSV 를 둘 다 받는다(시트가 여럿이면 골라 쓴다).

**변환 규칙은 `upload/index.html` 의 `cellStr`·`sheetRows` 한 곳뿐이다.** 알람 워크북 경로도
같은 함수를 지난다 — 두 벌이면 «알람은 되는데 실적은 날짜가 깨지는» 상태가 온다(제2원칙).
엑셀은 날짜·시각·숫자를 «셀 값»으로 들고 있어서, 문자열로 바꾸는 규칙이 CSV 관례와 조금만
달라도 조용히 갈린다. 실측으로 셋을 잡았고 전부 `tests/t-xlsx.mjs` 가 지킨다:

- **시각만 있는 칸을 엑셀은 1899-12-30 에 얹어 저장한다.** 그대로 찍으면
  `1899-12-30 15:00:00` 이 되어 CSV 의 `15:00:00` 과 다르다 → 1900년 미만이면 시각만 찍는다.
- **자정 정각이면 날짜만 찍는다.** `2026-08-14 00:00:00` 로 두면 CSV 로 올린 옛 행과
  «같은 날인데 다른 문자열» 이 되어 구간 교체(`left(...,10)`)·중복 판정이 어긋난다.
- **밀리초는 초로 반올림**하고 **부동소수 찌꺼기**(`0.30000000000000004`)를 떨어낸다.
  ⚠ 엑셀 시리얼 자체가 부정확해 타임스탬프가 CSV 와 **1초까지 다를 수 있다** — 초 단위를
  읽는 지표는 없으므로 무해하다. 「완전 일치」를 기대하고 대조하면 여기서 헷갈린다.

**천 단위 쉼표가 진짜 사고였다.** `Number('2,093')` 은 **NaN** 이다. 그래서 CSV 로 올린
자료는 **작업시간·공수·사용일이 1,000 이상인 행에서 값이 통째로 사라지고 있었다**
(빈칸과 구별되지 않는다). xlsx 는 쉼표가 없어 정상인데, 그러면 같은 자료가 올린 방식에 따라
다른 숫자를 낸다 — 그게 더 나쁘다. `GST.numv` 한 곳에서 흡수한다(빈칸은 0 이 아니라 NaN 이다).

**업로드는 «두 반쪽»이다 — 같은 파일을 두 번 올린다.** 알람과 올바이패스는 표가 둘이라
대상을 바꿔 두 번 올려야 한다. 실제로 올바만 올라가고 `sheet_alarm` 이 0행인 채로 지냈고,
그동안 화면은 국내를 수선실적 BM 으로 세면서 아무 말도 안 했다(수선실적은 알람유형 입력률이
0.3% 라 「미기재」 투성이가 되고, 그것을 코드 결함으로 오해하게 된다).
→ 업로드 화면이 `pair` 로 짝을 선언하고 **검사·완료 화면에서 짝 표의 현재 행수를 항상 보여준다.**
한 버튼으로 두 표를 다 넣지 않는 이유는 비우기-넣기가 표마다 원자적이어야 하기 때문이다 —
중간에 실패하면 한 표만 빈 채로 남는다.

**업로드는 워크북(.xlsx) 한 장을 통째로 받는다.** CSV 여섯 개로 쪼개면 어느 시트를 어느 표에
넣는지 사람이 틀린다. `/upload/` 가 시트 이름(`○운영_ALARM`)으로 골라 한 번에 넣는다.
xlsx 리더(SheetJS)는 **필요할 때만** CDN 에서 받는다 — 막혀도 CSV 경로는 멀쩡해야 한다.
SQL 은 `setup-10-alarm.sql`(선행: setup-8 · setup-9).

⚠ **이 워크북에는 작업자 실명(`담당자`·`확인자`)이 들어 있다.** DB 로는 가되(비공개·RLS)
화면에는 안 쓰고, **픽스처로 저장소에 넣지 말 것** — `t-alarm` 의 픽스처는 전부 지어낸 값이고
머리글 «이름»만 실물과 같다.

## 인원명단 양식이 세 벌이다 — 봇 사본이 제일 늦게 따라온다 (v96)

옛 영문(`Work Place`·`Date of entry`) · 한글(`ID`·`입사일`·`단지`/`라인`) ·
새 한글(`사원번호`가 맨 뒤, 옛 `ID` 는 빈 채로 남음). **세 벌을 다 받아야 한다.**

- **`kakao-bot/hr.js` 의 `parseRoster` 가 영문 양식 전용이라 한글 양식을 통째로 못 읽고 있었다.**
  `date of entry` 를 못 찾으면 `join` 이 undefined 라 아래 루프가 **전 행을 continue** 해
  0명이 되고, 봇은 「인원 데이터가 없습니다」라고 답한다 — 에러는 하나도 안 난다.
  대시보드(report·hr)는 진작 두 양식을 받고 있었는데 이 사본만 안 따라왔다(제2원칙 그대로).
- **Import 표는 ALTER 로 열을 «맨 뒤»에 붙인다.** 그래서 빈 `ID` 가 앞, 채워진 `사원번호` 가
  뒤에 온다. 이름 순서로 고르면 빈 열을 집어 사번이 통째로 비고 교육 매칭이 조용히 끊긴다 →
  **«실제로 채워진 열»을 고른다**(세 파서 공통).
- 칠러 제외 판정 자리도 이름이 바뀌었다 — 옛 `조직도` · 새 `담당구분`(Scrubber·Chiller).
- `tests/t-hr.mjs` 가 지어낸 CSV 세 벌로 지킨다(음성 대조 확인 — 별칭 하나만 빼도 8건이 깨진다).

## PPT 는 여덟 페이지가 한 양식으로 나간다 (v100)

**주간현황(report)** 은 실제 양식 파일(`report/qbr-template.pptx`)을 열어 차트 캐시·표 셀만
갈아 끼운다. **나머지 여섯**(설치·PM·고장·자재·CIP·TCO)은 `GST.pptAuto` **한 곳**이 만든다.
`GST.pageBar` 의 `caps.ppt`가 `'qbr'`(양식) / `'auto'`(공용)로 갈린다.

- **페이지마다 만들지 말 것.** 예전에 `pptAuto` 가 «어두운 바탕에 차트 한 장씩»이라
  hr 이 자기 것을 따로 짰고(`downloadHrPPT`), 같은 대시보드인데 장표 얼굴이 셋이 됐다.
  `t-ppt` 의 [4]가 여섯 페이지에 `new PptxGenJS(` 가 되살아나는 것을 막는다.
- **한 장에 최대 6개(3×2).** 숫자는 `GST.PPT_MAX_PER_SLIDE` 한 곳에 있다. 넘으면 슬라이드가
  늘고 제목에 `(2/3)` 이 붙는다. 마지막 장에 빈 칸이 남으면 **인사이트를 거기 담는다** —
  버리지도, 인사이트만 있는 장을 새로 만들지도 않는다.
- **차트는 캡처하는 동안만 «흰 종이용» 색으로 바꿨다 되돌린다**(`GST._chartLight`).
  · **옵션을 복제해 새 차트를 만들면 안 된다.** 축 눈금 콜백·플러그인이 함수라
    JSON 복제로 통째로 날아간다 — 실제로 그렇게 하면 x축이 타임스탬프 숫자로 찍힌다.
    `devicePixelRatio` 를 바꿨다 되돌리는 기존 방식과 같은 규율이다.
  · **되돌리기는 `finally` 에 둔다.** 중간에 던지면 화면 차트의 글자색이 검정으로 굳어
    어두운 테마에서 안 보이게 된다 — 내보내기를 한 번 눌렀을 뿐인데 화면이 상한다.

**오른쪽 위 「법인」 상자는 `GST.corpLabel()` 이 정본이다.**
양식 파일에는 `GST TAIWAN` 이 **세 장 모두에 박제**돼 있었다. 운영단위로 다른 법인을 골라도
숫자만 바뀌고 머리는 언제나 대만이라, **받아 본 사람은 대만 실적으로 읽는다.**

- **축 우선순위는 운영단위 → 국가 → 구분 → 전체.** 운영단위가 가장 좁으므로 그것이 이긴다.
  예전 `corpLabel` 에는 운영단위 축이 아예 없어서, 법인을 골라도 `GST Global` 이 그대로였다.
- **이름을 지어내지 않는다**(v98 그대로). 떼는 것은 **꼬리의 담당구분**(`SCRUBBER`·`CHILLER`)뿐이다 —
  `GST CHINA(WUHAN) SCRUBBER` → `GST CHINA(WUHAN)`. 앞부분을 손대면 중국 세 법인이 한 덩어리가 된다.
- **`data.corp` 를 안 주면 양식 원문을 그대로 둔다.** `qbr-ppt.js` 만 먼저 배포됐을 때
  상자가 «빈 칸»이 되지 않게 — 틀린 이름보다 낫지만, 빈 칸은 더 나쁘다.
- 페이지가 `corpLabel` 을 다시 정의하면 **표 왼쪽 칸과 PPT 머리가 다른 말을 한다**
  (`t-ppt` 가 막는다).

**업로드 버튼은 셸 툴바(새로고침 옆)에 있다.** `/upload/` 는 탭이 아니다 — 매일 보는 탭 줄에
관리용 화면을 섞지 않는다(`/diag/` 와 같은 규율). 그래서 링크가 없으면 사람이 못 찾는다.
**새 창으로 연다** — 탭 iframe 에 띄우면 탭 줄에서 아무 탭도 활성이 아니게 되고, 업로드 도중에
탭을 누른 순간 **진행 중이던 적재 화면이 그대로 날아간다**.

## 자동순회(키오스크)는 «남의 화면을 빌려 쓴다» (v101 · v102)

벽 화면·회의실 상시 표시용. 셸 툴바의 「자동순회」가 **단지 × 페이지**를 돌며 보여준다
(단지가 바깥 루프 — 한 단지의 이야기를 끝까지 보여 준 뒤 다음 단지로). 기본 15초.

**순회 기준 축은 고를 수 있다 — 단지 · 운영단위 · 고객사 (v102).** 「사이트별」을 사람마다
다르게 뜻한다: 국내는 단지(H1·P2), 해외는 법인(`GST TAIWAN`)이 곧 그 단위다.

- **값 목록을 셸에 박지 말 것.** 열려 있는 탭에 물어서 받는다(`gst-kiosk-q` →
  `GST.filters.options(축)`). 박으면 국내가 들어왔을 때 통째로 빠졌던 v89 가 그대로 재현된다.
- **«열린 모든 탭»에 묻고 합친다.** 활성 탭 하나에만 물었다가, 그 탭이 답을 못 하면
  패널이 「읽는 중…」에서 **영영 멈췄다** — 실제로 사용자가 하루 종일 그 화면을 봤다.
- **답이 없을 때 조용히 기다리지 말 것.** 2.5초로 끊고 «무엇을 하면 되는지»를 적는다.
  두 실패는 **할 일이 완전히 다르므로 다른 말을 해야 한다**:
  · **답이 하나도 없음** → 배포된 core.js 가 옛 버전이다. 「Ctrl+Shift+R 로 강제 새로고침」.
  · **답은 왔는데 목록이 빔** → 그 축에 값이 없는 것이다. 「다른 기준을 고르세요」.
    새로고침을 시키면 헛수고를 시키는 것이다. 그래서 답에 `ver`·`page` 를 실어 보낸다.
- **축마다 그릇이 다르다 — 단지는 Set, 나머지는 문자열.** 저장할 때 «생긴 대로» 담아야
  복원된다(`cur instanceof Set ? Array.from(cur) : cur`). `chosen()` 은 무엇이든 배열로
  눕히므로 그걸로 저장하면 **문자열 축이 배열이 되어 돌아온다.**
- **값 적용은 `GST.filters.set` 을 지나야 한다.** `F.campus` 에 직접 대입하면
  그다음 `.has` 가 TypeError 로 죽는데 **화면에는 아무 말도 안 뜬다**(v93 과 같은 자리).
  `set(k, '문자열')` 은 다중 축이면 알아서 Set 에 넣으므로 축을 안 가리고 쓸 수 있다.
- **끝나면 사람이 걸어 둔 필터를 돌려준다.** 저장은 셸의 «시작» 신호가 아니라 **처음
  건드릴 때** 한다 — 탭은 지연 로딩이라 순회 도중에 만들어지는 탭이 있고, 그 탭은 시작
  신호를 못 받는다. 복원은 **열린 모든 탭**에 보낸다.
- **지금 무엇을 보는지 화면에 적는다.** 벽에 걸면 지나가는 사람은 맥락 없이 숫자만 본다 —
  단지 이름이 없으면 그 숫자를 자기 단지로 읽는다. 하단바는 덮지 않고 **자리를 차지**한다
  (겹치면 표 마지막 줄·범례가 가려진다).
- **한 바퀴가 몇 분인지 적는다.** 「15초」만 보면 8페이지 × 6단지 = 12분이라는 것을 아무도
  모르고, 「왜 아까 그 화면이 다시 안 오나」로 돌아온다.
- **마우스가 움직였다고 멈추지 않는다.** TV 앞은 사람이 지나다니는 곳이다 — 멈추는 것은
  Esc 와 버튼뿐이다. 대신 순회 중에는 `wakeLock` 으로 화면이 잠들지 않게 한다
  (탭을 숨겼다 돌아오면 브라우저가 해제하므로 `visibilitychange` 에서 다시 잡는다).
- **전체화면은 셸 머리(상단바·탭바·챗봇줄)를 감춘다** — 모니터를 꽉 채우려면 그래야 한다.
  사이드바는 원래 접혀 있어 손댈 것이 없고, **페이지 자신의 머리는 남긴다**(기준일·필터 칩이
  없으면 벽에 걸린 숫자를 오늘 것으로 읽는다).
  · `requestFullscreen` 은 **사용자 동작 안에서만** 걸린다 — 시작 버튼 클릭 흐름에서 부른다.
    막히면 조용히 창 모드로 돈다(순회 자체는 그대로다).
  · **`fullscreenchange` 를 반드시 듣는다.** F11 로 전체화면만 빠져나갔을 때 머리가 감춰진
    채 남으면 **되돌릴 방법이 없어진다**.
- 자료 갱신은 따로 안 한다 — 페이지가 이미 `GST.startAutoRefresh(30)` 으로 30분마다 받는다.

## 자동순회가 «걸었다고 답하는지» 확인한다 (v103)

셸이 값을 던지기만 하면 **그 페이지에 없는 값은 조용히 «전체»로 풀린다** —
`fill`/`fillMulti` 가 «목록에 없는 선택값은 버린다»(사람이 고를 때는 옳은 규칙)라
빈 Set 이 되고, 빈 Set 은 `pass()` 에서 전체다. 하단바는 그대로 「H1」을 적으므로
벽 앞의 사람은 전사 합계를 H1 숫자로 읽는다.

- 페이지가 `gst-kiosk-ack {applied, why}` 로 답하고, 못 걸었으면 하단바가 밝힌다.
- 셸은 **탭별 목록**을 기억해 그 탭이 모른다고 답한 조합은 애초에 뺀다(뺀 개수도 적는다).
- ⚠ **«걸렸나»를 `F` 로 확인하면 안 된다.** 버리는 일은 DOM(`fill`)이 하므로 사이드바가
  없는 상황에서는 «걸린 척»이 된다. 물어야 할 것은 «이 페이지 «자료»에 그 값이 있나»다.
- **mount 전에 온 값은 보류한다.** 탭은 지연 로딩이고 `mount` 는 25만행을 다 받은 뒤에
  불리는데 메시지 리스너는 즉시 붙는다. 그 사이에 값을 넣으면 뒤늦은 `load()` 가 덮어
  «하단바는 H1, 화면은 남의 단지»가 되고, **순회를 끄면 사람 필터가 사라진다**(기준선이
  빈 값으로 잡혀서). 셸의 재전송으로는 원리적으로 못 맞춘다 — `GST.filters.kioskSet` 이
  보류했다가 `load()` 직후 적용하고 기준선도 그때 잡는다.
- `save()` 는 `KEY` 가 비어 있으면 쓰지 않는다 — «빈 이름» 키에 남의 페이지 값이 섞였다.
- 지연 재전송에는 **단계 토큰**을 단다. 정지 뒤에 도착하면 방금 돌려준 필터를 다시 덮는다.
- `beforeunload` 에서 복원한다 — 순회 중 셸을 닫으면 빌린 필터가 영영 안 돌아온다.

## 흰 종이용 캡처는 «밝은 글자»만 어둡게 한다 (v103)

자체 플러그인이 캔버스에 직접 찍는 글자(막대 위 값·도넛 가운데 TOTAL)는 색을 자기 옵션에
들고 있다. 어두운 테마 기본값이 밝은 색이라 흰 종이에 그대로 찍으면 **글자가 통째로
사라진다** — 렌더는 성공하므로 에러도 경고도 없다.

- **플러그인 이름을 나열하지 말 것.** 나열하면 새 플러그인이 생길 때마다 재발한다.
  물어야 할 것은 하나다 — «이 색이 흰 종이에서 안 보일 만큼 밝은가»(`GST._tooLight`,
  상대 광도 0.55). 빨강 이상치 표식(0.34)·회색 보조글자(0.31)는 그대로 둔다.
- **색을 «안 넘긴» 차트도 같이 본다.** 이 프로젝트의 플러그인 기본색은 전부 어두운
  테마용이라 «없음»도 «밝음»과 같은 뜻이다.
- **되돌리기는 «없던 키»를 지운다.** `undefined` 로 남기면 값은 같아 보여도
  `'color' in o` 가 달라져, 그걸로 판단하는 코드가 생기면 그때부터 조용히 갈린다.

## 양식 PPT 는 «화면 순서 그대로» 채운다 — 이름을 맞추려 들지 않는다 (v103)

가동현황 표는 양식 행 라벨(`Micron F16`·`Tong luo`…)과 대시보드 행 라벨(법인 또는
고객사)을 대만 전용 정규식으로 맞췄는데 **어느 모드에서도 안 맞았고, 못 맞춘 행은 양식에
박힌 대만 표본 숫자가 그대로 남았다.** 법인 상자에는 `SEC` 가 찍히므로 받아 본 사람은
삼성 실적이 961대라고 읽는다. TOTAL 만 갱신되니 합계와 위 행의 합도 안 맞았다.

- **순서대로 채우고 라벨까지 갈아끼운다** — PPT 가 화면과 같은 말을 한다.
- **남는 양식 행은 비운다.** 옛 숫자를 내보내느니 빈 칸이 낫다.
- 슬롯보다 행이 많으면 **몇 행이 빠졌는지 `notes` 로 알린다**(조용히 빠지면 아무도 모른다).
- **교육 표 머리글도 잡힌 인원을 따라간다**(v96 규약). 화면에 지금 떠 있는 그 글자를
  그대로 넘긴다 — 안 넘기면 양식 원문을 그대로 둔다(옛 호출부 보호).

## 쓰기 컬럼맵도 별칭 배열이다 (v104)

인원명단 양식이 세 벌이라 **읽기 세 곳은 별칭으로 흡수하는데 쓰기(`GST.DBW`)만 옛 영문
단일 이름**이었다. 그래서 v96 새 양식 이후 hr 의 인원 편집이 아무에게도 안 열렸고(빈 `ID`
열로 찾아 0행 → `not_found`), 신규 등록은 옛 열에 값을 넣고 «저장됨»이라 답해 **어느
화면에도 안 보였다** — 가장 설명하기 어려운 실패다.

- **순서가 규약이다 — 새 양식 먼저.** Import 표는 `ALTER` 로 열을 맨 뒤에 붙이므로
  빈 `ID` 가 앞, 채워진 `사원번호` 가 뒤에 온다. 이름 순서로 고르면 빈 열을 집는다.
- **`append` 는 표의 «실제» 열에서 고른다.** 다만 표가 비어 있으면 한 행 읽기로는 알 수
  없는데 **첫 등록이 바로 그 순간**이다 — 그때는 별칭의 첫 이름(정본)을 쓴다.

## CDN 을 기다리는 곳에는 «반드시» 시간 제한을 둔다 (v105)

사내망이 CDN 을 **«거부»하지 않고 «묵살»하면** `onerror` 가 영영 안 온다. `await` 가 그대로
멈추고, **버튼을 눌러도 아무 반응이 없다** — 에러도 경고도 없다. 실제로 그 증상으로
돌아왔다(PPT 버튼). 에러보다 나쁜 것이 «아무 일도 안 일어나는 것»이다.

- `GST.pptLoad`(15초) · `/upload/` 의 `xlsxLib`(15초) — 둘 다 같은 규율이다.
- **끝났을 때 «무엇을 하면 되는지»까지 적는다.** 「cdn.jsdelivr.net 을 막고 있을 수
  있습니다 → 전산팀에 허용 요청, 또는 차트별 「📋 복사」」. 막다른 길로 두지 않는다.
- **오래 걸리는 버튼은 즉시 잠근다**(`GST._barBusy`). 몇 초 동안 표시가 없으면 사람은
  계속 누르고, 그러면 동시에 여러 벌이 돈다.
  ⚠ 공통바 버튼은 **셸에 있고 클릭만 iframe 으로 온다** — 페이지에서 DOM 을 찾아도 없다.
    페이지가 `gst-bar-busy` 로 알리고 셸이 잠근다. 셸에는 **60초 안전장치**를 둔다:
    페이지가 죽으면 버튼이 영영 잠긴 채로 남는다.

**PPT 는 카드 «안의 모든» 캔버스를 담는다.** `.cw canvas` 만 보면 추이(`.trend-wrap`)·
크로스(`.cross-wrap`) 카드의 차트가 통째로 빠진다 — 실측 설치현황 14개 중 5개,
고장분석 32개 중 7개가 그 바깥이다. Chart 인스턴스가 없는 캔버스는 자연히 걸러진다.

**시트 주소가 아니라 gid 만 코드에 둔다 (v105).** 웹게시 토큰(`/d/e/2PACX-…`)이 9개 파일에
평문으로 박혀 있었다 — 웹게시에는 인증이 없어 URL 만 알면 로그인 없이 전량을 받는다.
사용자가 2026-08 에 게시를 해제해 통로는 닫혔지만, **문자열을 남겨 두면 새 파일이 복사해
간다**(실제로 `/diag/` 를 만들 때 그랬다). `GST.sheetUrl(gid)` 로 조립하고,
`t-leak` 이 토큰 «형태»를 막는다(대조본이 필요 없어 어느 환경에서도 돈다).

## 필터 사이드바는 «어느 자료에 걸리는지»로 묶는다 (v107)

축마다 있는 자료가 다르다. **사업부는 설비 쪽에만** 있고(인원현황은 열은 있으나 584행
전부 공란 — 실측), **팀은 인원 쪽에만** 있다. 한 줄로 늘어놓으면 「사업부를 바꿨는데
인원 숫자가 안 변한다」가 «고장»으로 읽힌다.

- 사이드바는 `GROUPS` 로 그린다 — 공통 / 「설비 기준」 / 「인원 기준」.
- ⚠ **집계 계층(`AXES`)은 그대로 둔다.** 화면 묶음과 한 배열로 합치면, 화면을 정리하려다
  종속·술어의 계층이 바뀐다. `t-filters` 가 «두 목록이 같은 축 집합인지»를 대조한다.
- 묶음 머리글 스타일은 core.js 가 한 번 주입한다 — 여덟 페이지의 `<style>` 을 각각
  고치면 한 곳이 빠져 그 페이지만 다르게 보인다.

**사업부는 조인해서 얻는다 (v107).** 설치현황에만 있는 열이라 실적·CIP 에는 없다.
`GST.ORG.instIndex` 가 `div` 를 들고, 각 페이지가 설비 S/N 으로 물려받는다
(fault·material·tco·cip·report). **조인은 파싱 직후 한 번만** — `rows()` 는 필터를
만질 때마다 불린다. 못 붙은 행은 빈 값으로 두고 `loose:{div:1}` 로 통과시킨다
(버리면 「사업부를 고르면 건수가 반으로 준다」가 된다). `pm` 은 설치현황을 안 읽으므로
「전체 (자료 없음)」으로 잠긴다 — 그 사실이 묶음 머리글로 설명된다.

**`refresh()` 는 `rows()` 를 «한 번만» 부른다 (v107).** 예전에는 축마다 불러 일곱 번이었다.
주간현황의 `rows()` 는 실적 25만 + 설치 + 인원을 합쳐 «새 배열을 짓는다» — 실측 한 번에
30ms 라 그것만 208ms 를 썼다. 축 값도 행마다 다시 뽑지 않고 **축별 평면 배열**로 한 번
뽑는다.
- ⚠ **행마다 객체를 만들면 오히려 느리다.** 7키 × 25만 개 할당이 스캔보다 비싸다 —
  실측으로 옛 방식(464ms)보다 나빠졌다(654ms). 문자열 배열 일곱이 답이다(250ms).
- 걸려 있는 축만 좁힘 계산에 쓴다(`ON`). 대개 한두 축뿐이라 이것만으로 대부분이 사라진다.

## 검증

`tests/`에 24종이 있다.

⚠ **초록불이 거짓말을 하지 않게 해 두었다.** 픽스처(실데이터 CSV)가 없는 환경에서
예전에는 ① 크래시로 `npm run all` 이 1번에서 죽거나 ② 핵심 대조를 «건너뛰고도 ✅» 로
끝났다. 실제로 그 초록불을 믿고 여러 번 작업했다. 이제 건너뛴 것이 있으면
「⚠️ 부분 검사 — N개 항목을 건너뛰었다」라고 다르게 말하고, `STRICT_FIXTURES=1` 이면
종료코드 2 로 실패한다(진짜 CI 용). 종료코드를 기본 0 으로 둔 이유는, 픽스처 없는
환경에서 멈추면 «돌 수 있는 나머지»까지 못 돌기 때문이다. 시트 파싱을 건드렸으면 최소 `t-sync.mjs`·`t-shift.mjs`·`t-mirror.mjs`,
분석 화면을 건드렸으면 `t-deep.mjs`를, **사이드바·필터를 건드렸으면 `t-filters.mjs`** 를 돌린다. 실행법과 픽스처 만드는 법은 `tests/README.md`.

| 스크립트 | 지키는 것 |
|---|---|
| `t-map.mjs` | 헤더 매퍼 단위 + 열 삽입·이름 변경 회귀 |
| `t-keys.mjs` | 페이지가 쓰는 컬럼 키가 전부 정의됐는지 |
| `t-hr.mjs` | hr.js 파서가 기존과 같은 결과를 내는지 |
| `t-sync.mjs` | **세 곳이 같은 열을 보는지** (가장 중요) |
| `t-smoke.mjs` | 6개 페이지 실제 렌더 · JS 에러 0 |
| `t-shift.mjs` | 시트에 열을 끼워 넣어도 KPI가 그대로인지 |
| `t-deep.mjs` | 심층 분석이 실측값과 같은 숫자를 그리는지 |
| `t-leak.mjs` | **공개 저장소에 실데이터가 새어 나갔는지** (설비 S/N·작업자 실명) |
| `t-mirror.mjs` | **미러가 시트와 같은 값을 내는지** (두 파싱 경로를 열별 체크섬으로) |
| `t-snap.mjs` | 큰 이행 전후로 8페이지 KPI·차트·표가 한 자리도 안 움직였는지 |
| `t-region.mjs` | **국내/해외가 들어와도 지표가 «조용히 0» 이 되지 않는지** (대만 전용 목록의 부활 차단) |
| `t-filters.mjs` | **여덟 페이지가 같은 기본 필터 한 벌을 쓰는지** · 국내 교육 «비대상» 규칙 |
| `t-alarm.mjs` | **국내 알람 워크북의 세 양식이 한 스키마로 눕는지** · 집계 대상 판정 |
| `t-ppt.mjs` | **PPT 가 한 양식으로 나가는지** · 법인 상자가 운영단위를 따라가는지 |
| `t-kiosk.mjs` | **자동순회가 단지 × 페이지를 돌고 필터를 돌려주는지** · 못 걸었을 때 밝히는지 (실제 브라우저) |

브라우저 경로는 `PW_CHROMIUM=/opt/pw-browsers/chromium-1194/chrome-linux/chrome` 로 넘긴다.

---

## 조직 3계층 — Customer · FAB · Floor (v76)

용어를 섞으면 화면이 거짓말을 한다. 계층은 `assets/core.js`의 **`GST.ORG`가 정본**이다.

| 계층 | 뜻 | 값 | 어느 열에서 오나 |
|---|---|---|---|
| **Customer** | 고객사 법인 | MICRON · PSMC · TASC · WINBOND | 실적 `고객사` · 설치 `Customer` |
| **FAB** | 공장 | F16 · F11 · F16N · F16S · F15 · F10 | 실적 **`라인`** · 설치 `FAB` |
| **Floor** | 동·층 (화면 표기는 **Line**) | `A3 M2 4F` · `B 2F` | **설치현황에만 있다** |

- **실적 시트의 `라인` 열은 사실 FAB이다.** 이름에 속지 말 것.
- **Floor는 수선·자재 실적에 열 자체가 없다.** `GST.ORG.instIndex`로 설치현황을 조인해
  얻는다(설비호기 → S/N 순). 실측 커버리지 수선 97.1% · 자재 85.7%.
  일본(F15_*)·싱가포르(F10*) 설비는 **설치현황 시트에 아예 없어 원리적으로 못 붙는다**(4,027행).
  고치려 들지 말고 **'미상'으로 세워 화면에 밝힌다.** 빈칸으로 두면 축에서 조용히 사라진다.
- **한 축에 두 차원을 섞지 말 것.** 예전 `report`의 `lineKeyOf`가 `fab + ' ' + customer`를
  한 문자열로 합쳐 7키로 뭉갰다. 그래서 Micron의 F16N(Tongluo)이 PSMC와 동급 형제가 되어
  "Tongluo도 마이크론인데 왜 따로 잡히나"가 나왔다. 실측은 명확하다 —
  설치현황에 `Customer=MICRON · Location=TONGLUO · FAB=F16N` 58건, 예외 0.
**국내/해외는 자료마다 근거가 다르다 — `GST.ORG` 가 정본이다 (v88).**
2026-08 에 국내(삼성·SK) 실적·인원이 들어오면서 «전부 대만»이라는 전제가 깨졌다.
실적은 `운영단위`(SEC·SDC=국내 / GST XXX=해외), 설치현황은 `Country`,
인원현황은 **국가 열이 아예 없어** 중문명·`Dept.`·사번을 다수결로 본다(`GST.ORG.emp`,
실측 421명 예외 0). 페이지마다 새로 짜지 말 것 — 갈라진다.
- **`Country` 열에 국가가 아닌 사업부명이 섞여 온다**(`GST HEFEI SCRUBBER` 등, 실측).
  그대로 쓰면 「국가」 축에 사업부명이 뜬다 → `GST.ORG.country()` 로 한 번 거른다.
- **근무지에는 FAB 코드만 적힌다**(`F16`·`F11`). 그래서 `country()` 가 FAB 도 국가로 푼다
  (F16/F11=대만 · F10=싱가포르 · F15=일본). 이 줄이 없으면 대만 인원 101명 중 6명만 잡혔다.
- 예전 `report` 의 `siteCountryStatic` 은 **값이 있으면 무조건 `TAIWAN`** 을 냈다.
  그래서 국가 필터를 걸어도 국내 인원까지 전원 통과해 **숫자가 안 변했다.**
  실적 쪽에도 `/F1[016]|PSMC|WINBOND/` 로 대만 사이트만 남기는 정규식이 있었다 —
  국내 실적이 집계에서 통째로 빠지는 원인이었다. 둘 다 걷어냈다.

**«대만 전용 목록»은 report 에만 네 군데 더 있었다 (v89).** 국내 필터를 걸면
**에러 하나 없이 KPI 가 전부 0** 이 되던 원인이다. 전부 «목록»에서 «규칙»으로 바꿨고
`tests/t-region.mjs` 가 목록 리터럴의 부활을 소스 단위로 막는다.
- `QBR_SITE` — 운영지표 분모의 **허용목록**이었다. 국내 근무지(P1·A3·청주…)가 하나도 없어
  재직 인원·공수·인당 챔버가 0. → **제외목록**(`TSMC` + 근무지 미기재)으로 뒤집었다.
  실측 585행에서 대만 판정은 그대로다(떨어지는 것은 TSMC 1명뿐).
- `SITES` 고정 6키 — 스택 막대의 계열 목록. **목록에 없는 사람은 합계에서 조용히 사라진다.**
  → 데이터에서 만들되 상위 11개 밖은 **`기타`로 반드시 담는다**(버리지 않는다).
  팔레트는 6색뿐이라 그 너머는 색상환으로 잇는다 — `undefined` 색이면 막대가 투명해진다.
- 설치 `FAB` 정규식 — TO·챔버 산식의 모집단. 국내 설비 7,337대가 전부 탈락했다.
  → «FAB 값이 있으면 통과». 대만 1,515대는 이 규칙으로도 100% 통과한다.
  **부수 발견**: 옛 정규식은 FAB 열만 봐서 PSMC(FAB 이 `P1`·`P2`·`P3`)·TASC(`FAB-4B`)
  **39대를 TO 산식에서 빼고 있었다.** 가동현황 표는 같은 설비를 세고 있었으니 두 카드가
  서로 다른 모집단이었다 — 고치면 대만 TO 가 그만큼 는다(줄어든 게 아니라 원래 빠져 있던 것).
- `OROWS` — 가동현황 표의 행 목록. → 현재 필터에 실제로 있는 구분으로 만든다(최대 12행).
  ⚠ 설치는 FAB·고객사에서, 인원은 근무지에서 키가 나온다. 대만은 어휘가 같아 맞물리지만
  **국내는 설비가 `삼성전자`·인원이 `P1`** 이라 안 붙는다 → 그 행의 인원 칸은 `-` 로 남는다.
  억지로 잇지 말 것. 매핑표가 생기기 전까지는 **빈 칸이 곧 «자료가 없다»는 사실**이다.
- 표·PPT 머리의 법인 표기 기본값이 `GST Taiwan` 이었다 → `corpLabel()`(필터를 따라간다).
- `headL` 만 `okCF`(구분 조건 없음)를 써서, 국내를 걸어도 **총인원 차트에만 대만 값이 남았다.**
  → `okP`. 같은 모집단을 두 술어로 거르면 카드끼리 숫자가 갈린다.

**`GST.ORG.region()` 은 국가로도 역산한다.** 옛 설치현황 `Country` 는 `TAIWAN` 처럼
국가명이 그대로 들어 있어 `^GST|^SEC` 어휘 판정에 걸리지 않았다 → 그 행이 전부
«구분 미상»이 되어 **해외를 걸면 설비가 하나도 안 나왔다.** 국가를 알면 구분도 아는 것이다.

**`GST.VER` / `GST.needVer(n)` (v89).** 페이지는 배포본 core.js 를 절대경로로 부르므로,
core.js 만 캐시에 옛 것이 남으면 새 API 호출이 TypeError 로 죽고 화면은 «숫자가 전부 0» 으로만
보인다 — 단서가 하나도 없는 실패다. 페이지가 필요한 버전을 선언하면 붉은 배너가 뜬다.
**기능을 추가하면 `GST.VER` 를 올리고, 그 API 를 쓰는 페이지의 `needVer()` 도 같이 올린다.**

- **FAB 목록을 하드코딩하지 말 것.** 고정 7키가 F10·F15를 몰라 **자재 4,027건(13.6%)이
  필터에서 통째로 사라지고 있었다.** 원인은 `\bF10\b`가 `F10A`에서 `A`가 word char라
  매치에 실패한 것. `GST.ORG.fab`은 그래서 단어경계를 쓰지 않는다.

## 기본 필터는 여덟 페이지가 한 벌을 나눠 쓴다 — `GST.filters` (v91)

**구분 · 운영단위 · 고객사 · 단지 · 라인 · 팀 · 기간.** 이 일곱은 `assets/core.js`의
`GST.filters`가 만든다. 페이지는 «자기 데이터의 어느 필드가 그 축인지»만 알려 준다:

```js
GST.filters.mount({ page:'fault', rows:()=>ROWS, onChange:render,
  get:{ region:x=>x.region, op:x=>x.op, customer:x=>x.customerN,
        campus:x=>x.campus, line:x=>x.line, date:x=>x.work },
  drop:{ customer:GST.FILT_DROP_CUST, campus:GST.FILT_DROP_ORG, line:GST.FILT_DROP_ORG } });
```

그 뒤 술어 맨 앞에 `GST.filters.pass(x)` 한 줄. 공통 블록은 `.slicers` 맨 앞에 끼워지고
원래 있던 항목은 `.slicer-div` 아래 «이 페이지 전용»으로 밀려난다. 사이드바 자체는
페이지가 만들지 않는다 — `.slicers`를 두면 `GST.autoSidebar()`가 `<aside>`로 옮긴다.

**왜 이렇게까지 하는가.** 여덟 페이지가 사이드바를 각자 손으로 짰다. 그래서 같은
대시보드인데 페이지를 옮길 때마다 필터 항목·이름·동작이 달랐고(주간=구분/국가/고객사 ·
고장=고객사/FAB/Line · 인원=칩 · 설치=Country/Customer), 국내 자료가 들어오자 갈 곳 없는
값들이 고객사 목록으로 흘러들었다(인원현황 근무지가 고객사 칸에 뜨던 일).
`tests/t-filters.mjs`가 «다시 갈라지는 것»을 소스 단위로 막는다.

지킬 것 여섯:

- **없는 축도 칸은 남긴다.** 사용자 확정 — 자료에 값이 없으면 `전체 (자료 없음)`이라
  적고 잠근다. 칸이 나타났다 사라지면 그것 자체가 «페이지마다 필터가 다르다»로 읽힌다.
- **기간 칸은 `date` 접근자를 준 페이지에만 열린다.** tco의 기준 월, hr·cip처럼 자기
  시간축이 따로 있는 화면은 접근자를 주지 않아 칸이 잠긴다. 예전에는 접근자가 없어도
  기간 조건을 걸어, 날짜를 넣는 순간 화면이 통째로 비었다.
- **목록에서만 거른다 — 행은 남긴다.** `GST.FILT_DROP_ORG`(라인 칸에 든 `라인장`·`주재원`)
  와 `GST.FILT_DROP_CUST`(고객사 칸에 든 `본사`·`칠러`). 설치현황 `GST CHINA` 1행이
  목록에 떠 고르면 화면이 통째로 비던 것과 같은 부류다.
- **종속(cascading)은 자기 자신을 뺀 나머지로 좁힌다**(`passExcept`). 자기까지 보면 한 번
  고른 값 말고는 목록에서 사라져 되돌릴 수 없다.
- **일곱 축이 «전부» 다중선택이다 — Set 이다 (v106 · 사용자 요청).** 국내 설비는 물리적
  위치와 «관리주체(사업부)»가 따로 논다 — 같은 11라인 설비라도 메모리냐 연구소냐
  파운드리냐에 따라 소속 단지가 H3·H2·K1 로 갈린다. 하나씩만 고를 수 있으면
  «그 사업부의 설비가 실제로 어디에 있나»를 볼 수가 없다.
  · **축 목록은 `AXES` 한 곳에서 나온다.** 종속·사이드바 마크업·술어가 같은 목록을 봐야
    한다 — 예전에는 세 곳에 손으로 적혀 있어 하나만 고치면 조용히 갈라졌다.
  · **`if(F.op)` 같은 진위 검사를 쓰면 안 된다 — 빈 Set 은 truthy 다.** 반드시 `hasK`/`has`.
    술어는 `for(k of AXES){ if(hasK(k) && !axOk(k,x)) return false; }` 한 벌뿐이다.
  · **`corpLabel`·`filtSummary` 처럼 «이름을 적는» 곳은 Set 을 눕혀 본다.** `String(Set)` 은
    `'[object Set]'` 이다. 여럿 고르면 이름을 지어내지 않고 개수로 적는다(v98 규약).
  · 페이지의 드릴이 `GST.filters.F.line=(F.line===v)?'':v` 로 짜여 있던 곳이 fault·material·
    cip 에 다섯 곳 있었다 — 검사가 campus 만 보고 있어 통과했다. 이제 `t-filters` 가
    **전 축**으로 그 패턴을 막는다.
- **단지는 v93 부터 다중이었다.** 같은 설비의 단지가 자료마다 갈리기 때문이다
  (알람 시트 H3 ↔ 설치현황 H4, 실측 39건). 정본은 설치현황이지만 사람은 두 단지를
  한 번에 봐야 한다. 다른 축을 다중으로 바꾸려면 `MULTI` 에 이름만 더한다.
  · **`x.campus===F.campus` 로 직접 비교하지 말 것.** Set 을 `===` 로 보면 언제나 false 라
    필터를 거는 순간 화면이 통째로 빈다 — 에러는 하나도 안 난다. 판정은 `GST.filters.hit`
    (페이지 안에서는 `campOk` 같은 이름 하나)만 지난다.
  · **`GST.filters.F[k]=''` 도 금지.** Set 이 문자열로 바뀌어 그다음 `.has` 가 TypeError 다.
    `GST.filters.set(k,v)` · `.toggle(k,v)` 를 쓴다.
  · 저장은 배열로 눕히고 복원할 때 Set 으로 되돌린다 — `JSON.stringify(new Set)` 은 `'{}'` 다.
  · `tests/t-filters.mjs` 의 [7-3]이 여덟 페이지 소스에서 위 두 금지를 막는다.
  · **박스가 «열리는지»는 소스로 못 본다.** `.slicer` 에 `position` 이 없으면 박스가
    `position:fixed` 인 사이드바를 기준 삼아 `top:100%` = 사이드바 «바닥» 에 열려
    화면 밖으로 나간다 — JS 에러도 경고도 없이 «눌러도 아무 일이 없는 필터» 가 된다.
    `t-smoke` 가 실제로 눌러 좌표로 확인한다(음성 대조로 잡히는 것을 확인했다).
  · **박스의 Set 을 «갈아끼우지» 말 것.** `GST.mselFill` 이 만든 핸들러는 그 Set 에
    체크를 넣는다. 저장본 복원 등으로 `F.campus = new Set(...)` 하면 그때부터 클릭이
    옛 Set 으로 들어가 **첫 항목만 되고 두 번째부터 안 먹는다** — 에러가 하나도 안 난다.
    `load()` 는 비우고 다시 채우고, 핸들러는 `GST._msel[id]` 에서 «지금의» Set 을 꺼낸다.
    주간현황은 `mount` 를 5번 부르기 때문에 이 조건이 **반드시** 성립한다.
  · 사이드바의 `.slicer input{width:100% !important}` 가 체크박스까지 잡는다 —
    되돌려 놓지 않으면 체크박스 하나가 182px 이 되고 글자가 반대편 끝으로 밀린다.
  · **`t-smoke` 는 주간현황을 포함해야 한다.** 위 결함은 mount 를 여러 번 부르는
    페이지에서만 나는데, 그때 smoke 에 주간현황이 없어 여섯 페이지가 전부 통과했다.
- **비율 지표의 «분모» 배열에도 같은 축을 실어라.** 고장분석 `ALLW_ROWS`에 구분·운영단위·
  단지가 안 실려 있어, 구분을 고르면 설비 BM율·좌우 편중의 분모가 0이 됐다(에러 없음).
  PM은 `SCHED`·`DONE_HIST`·원본행 셋이라 `rowOrg` 하나로 같은 이름을 단다.
- **축을 화면에 내주면 드릴도 `GST.filters`를 토글한다.** 페이지의 `F`를 건드리면 막대를
  눌러도 아무 일이 안 일어난다. 그리고 막대 라벨은 필터가 비교하는 값과 «같은 정규화»여야
  한다 — 설치현황 고객사 막대가 원문으로 세고 있어 눌러도 0건이 되던 자리다.

**사업부(관리주체)는 «설치현황에만» 있다 (v106).** 수선·자재 실적에는 열 자체가 없다.
예전에는 실적 행에 `div:''` 를 넣었는데 사업부가 `loose` 축이라 빈 값은 전부 통과한다 —
그래서 **사업부를 바꿔도 실적 기반 건수가 하나도 안 움직였다**(사용자 보고).
`GST.ORG.instIndex` 가 이제 `div`·`campus` 도 들고, 설비 S/N 으로 조인해 실어 준다
(Floor 와 같은 규약). **조인은 파싱 직후 한 번만** 한다 — `rows()` 는 필터를 만질 때마다
불리므로 거기서 조인하면 25만 행 × 매 조작이 된다. 못 붙은 행은 빈 값 그대로 두어
loose 로 통과시킨다 — 버리면 「사업부를 고르면 실적이 반으로 준다」가 된다.

**CIP·인원처럼 조직 축이 원본에 없는 자료는 조인해서 얻는다.** CIP는 설비 S/N(없으면
Scrubber CODE)으로 설치현황을 조인한다. **못 찾은 행을 버리지 말 것** — 단지·라인은
자기 사이트(F11·F16)로 폴백한다. 버리면 «필터를 걸면 표가 반으로 준다»가 된다.

**국내에는 법인 교육과정(Basic·Veteran)이 없다.** 미이수가 아니라 **비대상**이다.
⚠ **판정을 여러 곳에 흩지 말 것.** 주간현황은 교육 계획 표·`cEdu` 차트가 `eduKey` 를 지나는데
**KPI 카드만 `'bdate'`·`'vdate'` 를 박아 두고 있었다** — 국내를 걸면 그 카드만 늘 0% 였다
(국내 인원에게는 Basic·Veteran 완료일이라는 것이 아예 없다). 같은 화면의 카드끼리 다른 답을
내는데 에러는 하나도 안 난다. **이름표도 잡힌 인원을 따라간다** — Lv.3 을 세면서 「Veteran」
이라고 적으면 그 카드는 거짓말을 하는 것이다(`t-filters` [7] 이 둘 다 막는다).
그렇게 안 세면 이수율이 국내 인원 수만큼 깎인다(실측 16.2% → 89.8%). 적용 여부는
`hr`의 `hasCorp` 한 곳에서만 판정하고, 대상이 없으면 그 막대를 **아예 그리지 않는다** —
0짜리 막대는 «못 채운 것»으로 읽힌다. 국내의 교육 축은 Scrubber Lv.2 · Lv.3 이고,
주간현황 교육 계획 표의 **머리글도 잡힌 인원을 따라간다**(안 그러면 Lv.2 숫자를 세면서
Basic이라고 말하는 표가 된다).

**다중선택 박스는 목록이 바뀔 때만 다시 만든다**(`GST.mselFill`). 매 렌더 innerHTML을 갈면
체크 직후 노드가 분리돼 목록이 닫히고, 반대로 '최초 1회만'으로 두면 데이터 로드 전에
빈 목록으로 굳는다. 둘 다 실제로 겪었다.

**Set을 상태에 넣으면 저장을 확인할 것.** `JSON.stringify(new Set)`은 `'{}'`이다.
`GST.encodeState`가 배열로 눕히고 `GST.applyState`가 타입을 지켜 되살린다 —
`Object.assign`으로 복원하면 `F.fab`이 빈 객체가 되어 `.has`가 사라진다(실제로 죽었다).

---

## 심층 분석을 고칠 때 지킬 것

건수 순위표는 쓸모가 없다 — **자재는 O-RING이 44%**라 어떤 축으로 잘라도 1위가 O-RING이다.
그래서 **집중지수(lift) = 관측 ÷ 기대**로 본다. 기대는 `행합 × 열합 ÷ 총합`, 표의 주변합만으로 구한다.

여기서 조용히 틀리는 자리가 셋 있다. 전부 실제로 한 번씩 겪었다.

- **집중지수는 더할 수 있는 값에만 쓴다.** 중앙값끼리 나누면 단위가 1/일이 되고,
  고유수는 주변합이라는 것 자체가 없다. `core.js`의 `fmtV`가 `agg`가 `count`·`sum`일 때만
  배수를 계산하고 나머지는 원값으로 폴백한다 — 측정값을 추가할 때 이 가정을 깨지 말 것.
- **월 축은 달력으로 채운다.** 자료가 있는 달만 이으면 25-09와 26-03이 나란한 막대로 붙어
  "꾸준히 늘다 튀었다"처럼 읽히고, '직전 6개월 평균'이 '직전 6개 항목 평균'으로 바뀌어
  배수가 부풀려진다. 판정은 `material`의 `monthSurges()` **하나만** 쓴다 —
  막대와 표가 각자 판정하던 시절, 같은 문구를 달고 서로 반대 결론을 내는 조합이 110건 있었다.
- **재교체 간격은 같은 날을 1회로 묶는다.** 한 번의 정비에서 O-RING을 여러 개 갈면 줄이
  그만큼 생긴다(같은 날 중복 6,266조합·최대 하루 20건). 안 묶으면 중앙값이 1일로 내려앉는다.

**축을 화면에 내주면 필터도 같이 만든다.** 심층분석이 '모델' 축을 제공하는데 `F.model`이 없어,
줄을 눌러도 모델 조건이 조용히 버려지고 자재만 걸린 적이 있다. 새 축을 추가하면
`F` · `filt()` · `filtAxis()` · `FIELD_LABEL` · 빵부스러기 · 드릴 맵을 **전부** 손댄다.
한 곳만 빠져도 화면의 카드들이 서로 다른 모집단을 보여준다.

**BM율(발생률)과 BM 점유율은 다른 질문에 답한다 — 병기하되 섞지 말 것.**
발생률(BM÷그 축의 전체 작업)은 "어디가 유독 잘 고장나나", 점유율(BM÷전체 BM)은
"고장이 어디서 많이 나나". 점유율만 보면 작업량 큰 축이 항상 1등이라(건수 순위표와
같은 문제) 발생률을 지웠다간 ALDNIT 33.5% 같은 신호를 놓친다. 반대도 마찬가지다.

**차트 세부내역은 `chart.$rows` 규약 하나로 단다(v83).** core.js의 gstRows 플러그인이
클릭을 받아 `GST.rowsModal`을 연다 — 차트를 만든 뒤
`CHARTS.x.$rows=(di,i)=>({title,cols,rows})` 한 줄이면 끝. 페이지마다 클릭 핸들러를
새로 짜지 말 것(hr처럼 `Chart.defaults.onClick`을 덮는 페이지에서 조용히 사라진다).
hline·예측 막대는 별도 데이터셋/꼬리 인덱스로 들어오므로 `di!==0`·범위 밖 인덱스를 걸러라.

**비율 지표는 단계 필터를 받으면 안 된다.** BM만 남기면 BM율이 100%가 된다.
`fault`는 그래서 `filt()`가 아니라 `filtAxis()`(라인·모델·설비·공정·기간만)를 쓰고,
분모는 서비스 4단계가 아니라 전 작업단계 원장(`ALLW_ROWS`)이다.
그리고 **분자와 분모가 같은 필터 집합을 쓰는지** 반드시 확인한다(`filtUPM` ↔ `filtTBM`).

> 이 파일들은 한 번에 여러 사람이 손대면 반드시 어긋난다. 위 결함은 전부
> **서로 다른 사람이 쓴 코드가 맞닿는 지점**에서 났다. 남의 배열(`ALLW_ROWS` 등)에
> 의존하는 카드를 추가했으면, 그 배열에 필요한 필드가 실려 있는지 직접 확인할 것.

---

## 코드 관례

- 페이지는 각자 `index.html` 하나에 자족한다(HTML·CSS·JS 인라인). 공용은 `assets/core.js`.
- i18n은 각 페이지 `T` 객체에 `ko`·`en`·`zh`·`ja` 네 언어. 문구를 추가하면 넷 다 채운다.
- 차트는 Chart.js. **날짜 어댑터를 싣지 않으므로 `type:'time'`을 쓸 수 없다** —
  타임스탬프를 `linear` 축에 넣고 눈금 콜백으로 날짜를 찍는다.
- S/N 셀 클릭은 `core.js`의 설비 드릴다운 메뉴가 캡처 단계에서 선점한다.
  페이지가 따로 클릭 핸들러를 달아도 실행되지 않는다 — `GST.snMenuExtra`에 항목을 추가할 것.
- 주석은 한국어로, **무엇을 하는지가 아니라 왜 그런지**를 적는다.
- 배수는 `GST.xmul`로 찍는다 — `4.5배`(en `4.5x`). `×`는 곱하기로 읽힌다.
  색은 `GST.xcol`(3배↑ 노랑 · 10배↑ 빨강).
- 월별 급증 판정은 `GST.monthSurges` **하나만** 쓴다. 두 벌이면 반드시 갈라진다.
