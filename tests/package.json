{
  "name": "gst-dashboard-tests",
  "private": true,
  "type": "module",
  "description": "시트 열 매핑 검증. 루트 배포에는 영향이 없다 — tests/ 안에서만 쓴다.",
  "scripts": {
    "map": "node t-map.mjs",
    "keys": "node t-keys.mjs",
    "hr": "node t-hr.mjs",
    "sync": "node --experimental-strip-types t-sync.mjs",
    "smoke": "node t-smoke.mjs",
    "shift": "node t-shift.mjs",
    "deep": "node t-deep.mjs",
    "leak": "node t-leak.mjs",
    "snap": "node t-snap.mjs",
    "mirror": "node --experimental-strip-types t-mirror.mjs",
    "region": "node t-region.mjs",
    "filters": "node t-filters.mjs",
    "all": "npm run map && npm run keys && npm run hr && npm run edu && npm run sync && npm run smoke && npm run shift && npm run deep && npm run leak && npm run mirror && npm run csvdb && npm run dbw && npm run region && npm run filters && npm run snap",
    "edu": "node t-edu.mjs",
    "csvdb": "node t-csvdb.mjs",
    "dbw": "node t-dbw.mjs",
    "upload": "node --experimental-strip-types t-upload.mjs",
    "botdb": "node --experimental-strip-types t-botdb.mjs"
  },
  "devDependencies": {
    "chart.js": "4.4.1",
    "papaparse": "5.4.1",
    "playwright": "^1.40.0"
  }
}